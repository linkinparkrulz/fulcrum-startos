import { Affine } from "../util";
declare const pathValue: unique symbol;
export type PathValue = typeof pathValue;
export type PathBuilderStored<AllStore, Store> = {
    [K in PathValue]: [AllStore, Store];
};
export type PathBuilder<AllStore, Store = AllStore> = (Store extends Record<string, unknown> ? {
    [K in keyof Store]: PathBuilder<AllStore, Store[K]>;
} : {}) & PathBuilderStored<AllStore, Store>;
export type StorePath = string & Affine<"StorePath">;
export declare const extractJsonPath: (builder: PathBuilder<unknown>) => StorePath;
export declare const pathBuilder: <Store, StorePath = Store>(paths?: string[]) => PathBuilder<Store, StorePath>;
export {};
